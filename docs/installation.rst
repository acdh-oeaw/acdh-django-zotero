============
Installation
============

At the command line::

    $ easy_install acdh-django-zotero

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv acdh-django-zotero
    $ pip install acdh-django-zotero
